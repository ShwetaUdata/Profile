const Header = () => {
  return (
    <header className="bg-gradient-to-r from-primary via-accent to-primary text-primary-foreground shadow-lg sticky top-0 z-40 backdrop-blur-sm">
      <div className="container mx-auto px-4 py-6">
        <h1 className="text-2xl md:text-3xl font-heading font-bold text-center tracking-tight">
          Welcome to My Portfolio
        </h1>
      </div>
    </header>
  );
};

export default Header;
